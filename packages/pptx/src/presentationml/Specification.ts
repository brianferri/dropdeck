/* eslint-disable @typescript-eslint/naming-convention -- ST_* mirror the ECMA-376 schema names verbatim. */

export enum ST_TransitionSpeed {
    Slow = "slow",
    Medium = "med",
    Fast = "fast"
}

export enum ST_TransitionSideDirectionType {
    Left = "l",
    Up = "u",
    Right = "r",
    Down = "d"
}

export enum ST_TransitionEightDirectionType {
    Left = "l",
    Up = "u",
    Right = "r",
    Down = "d",
    LeftUp = "lu",
    RightUp = "ru",
    LeftDown = "ld",
    RightDown = "rd"
}

/* eslint-enable @typescript-eslint/naming-convention */
